<h1 class="text-center my-5">Partie : <b>{{ game.mode }}</b> ({{ game._id}})</h1>

<div class="mb-5 d-flex justify-content-around">
    <a href="/games" class="btn btn-primary btn-lg">Gérer les parties</a>
</div>
<div class="main">
    <div class="d-flex justify-content-around">
        <div class="col-4 game_players">
             <h1>Joueurs dans la partie</h1>
            <table class="table table-striped table-bordered">
            <tr>
                <th>Nom</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
            {{#each players}}
            <tr>
                <td>{{name}}</td>
                <td>{{email}}</td>
                <td class="d-flex justify-content-between">
                <form method="POST" action="/games/{{ ../game._id }}/players">
                    <input type="hidden" value="{{ ../game._id }}" name="game_id">
                    <input type="hidden" value="{{ _id }}" name="player_id">
                    <button type="submit" class="btn btn-primary">
                    <i class="fas fa-plus"></i>
                    Ajouter
                    </button>
                </form>
                <form method="POST" action="#">
                    <button class="btn btn-warning" type="submit">Supprimer</button>
                </form>
                </td>
            </tr>
            {{/each}}

            </table>
        </div>
        <div class="col-4 no_game_players">
             <h1>Joueurs sans game</h1>
            <table class="table table-striped table-bordered">
            <tr>
                <th>Nom</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
            {{!-- {{#each players}}
            <tr>
                <td>{{name}}</td>
                <td>{{email}}</td>
                <td class="d-flex justify-content-between">
                <form method="POST" action="/games/{{ ../game._id }}/players">
                    <input type="hidden" value="{{ ../game._id }}" name="game_id">
                    <input type="hidden" value="{{ _id }}" name="player_id">
                    <button type="submit" class="btn btn-primary">
                    <i class="fas fa-plus"></i>
                    Ajouter
                    </button>
                </form>
                <form method="POST" action="#">
                    <button class="btn btn-warning" type="submit">Supprimer</button>
                </form>
                </td>
            </tr>
            {{/each}} --}}
            </table>
        </div>
    </div>
</div>
